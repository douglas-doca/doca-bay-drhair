{
  "name": "doca-scheduler-http",
  "version": "2.0.0",
  "description": "DOCA Scheduler — UnObject GraphQL HTTP (sem Puppeteer)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test:auth": "node scripts/test-auth.js",
    "test:slots": "node scripts/test-slots.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
